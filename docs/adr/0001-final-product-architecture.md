# 使用账户所有权的采访记录单体架构

系统采用 React + Cloudflare Worker + D1 的模块化单体。Cloudflare Access 负责身份验证，D1 账户与 `record_owners` 负责授权。成员直接维护自己的采访记录；认领获批后成为共同主人；馆长具有全局权限。不设置内容复核角色。

录入是独立页面和模块，来源由 `source_type` 描述，不与抖音或单一表单耦合。公开时生成不可变版本，修改通过修订号防止覆盖，删除采用软删除并写审计事件。

这是产品上线前收敛的唯一方案。数据库、类型、页面和 HTTP 路径不提供旧定义或兼容层。
