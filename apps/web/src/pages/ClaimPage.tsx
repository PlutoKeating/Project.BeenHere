import { useState } from "react";
import { Link, useParams } from "react-router-dom";
import { api } from "../lib/api";

export function ClaimPage(){const {recordId=""}=useParams(),[text,setText]=useState(""),[status,setStatus]=useState<string|null>(null),[busy,setBusy]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);try{await api.submitClaim(recordId,text);setStatus("认领申请已提交。记录主人会阅读你的说明并决定是否通过。");}catch(cause){setStatus(cause instanceof Error?cause.message:"提交失败。");}finally{setBusy(false);}}return <section className="reading-column px-5 py-14 tablet:px-0 tablet:py-20"><p className="record-label text-blueprint">CLAIM REQUEST</p><h1 className="mt-4 font-serif text-4xl font-semibold">认领关于自己的采访记录</h1><p className="mt-6 text-sm leading-8 text-ink-muted">认领不是自动通过。请说明你与被采访者的关系、可用于核实身份的信息，以及认领后准备如何维护这条记录。原记录主人将审阅全文。</p>{status?<div className="paper-card mt-8 p-6"><p className="leading-7">{status}</p><Link to="/studio/claims" className="button-primary mt-6">查看申请</Link></div>:<form onSubmit={submit} className="paper-card mt-8 space-y-6 p-6 tablet:p-8"><div><label className="field-label" htmlFor="claimText">申请说明</label><textarea id="claimText" className="field min-h-48" required minLength={20} maxLength={2000} value={text} onChange={e=>setText(e.target.value)} placeholder="我是这条采访记录中的被采访者……"/></div><button className="button-primary w-full" disabled={busy}>{busy?"正在提交…":"提交认领申请"}</button></form>}</section>}
